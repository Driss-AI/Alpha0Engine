# Single dependency set for the whole engine (api + pipeline) — one image, one lockstep.
# Union of the former per-service requirements, minus deps owned by removed
# subsystems (boto3/R2, rapidfuzz+tldextract/entity-resolver, redis).
aiosqlite==0.20.0
alembic==1.18.4
asyncpg==0.29.0
fastapi==0.111.0
httpx==0.27.0
pandas==2.2.2
psycopg2-binary==2.9.9
pydantic==2.7.1
python-dotenv==1.0.1
requests==2.31.0
schedule==1.2.2
slowapi==0.1.9
sqlmodel==0.0.19
uvicorn[standard]==0.29.0
yfinance==0.2.40
